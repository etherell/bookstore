.row
  = render books.decorate
- if books.next_page
  .text-center.view-more-btn-container
    = link_to request.params.merge(page: books.next_page), class: 'load-more', remote: true do
      %button.btn.btn-primary= t('.view_more')
